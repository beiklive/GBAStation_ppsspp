name: Build GBAStation PPSSPP for Nintendo Switch

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-switch:
    runs-on: ubuntu-latest
    timeout-minutes: 180
    container:
      image: devkitpro/devkita64:latest
    steps:
      - name: Check out PPSSPP source
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: recursive

      - name: Install Switch and switchVK dependencies
        shell: bash
        env:
          SWITCHVK_TOKEN: ${{ secrets.SWITCHVK_READ_TOKEN }}
        run: |
          dkp-pacman -S --noconfirm --needed switch-dev switch-curl
          if [[ -n "${SWITCHVK_TOKEN}" ]]; then
            git clone --depth=1 "https://x-access-token:${SWITCHVK_TOKEN}@github.com/beiklive/switchVK.git" "$GITHUB_WORKSPACE/../switchVK"
          else
            git clone --depth=1 https://github.com/beiklive/switchVK.git "$GITHUB_WORKSPACE/../switchVK"
          fi
          bash "$GITHUB_WORKSPACE/../switchVK/ci/install-linux-deps.sh"

      - name: Prepare switchVK build inputs
        shell: bash
        run: |
          SWITCHVK_CI_BUILD_ROOT="$GITHUB_WORKSPACE/../switchVK/.ci-build" \
          MESA_26_1_4_PACKAGE="$GITHUB_WORKSPACE/../switchVK/nvk-switch-26.1.4" \
            bash "$GITHUB_WORKSPACE/../switchVK/ci/build-sdk-linux.sh"

      - name: Prepare PPSSPP flash0 assets
        shell: bash
        run: |
          set -euo pipefail
          assets_commit=0f19aedc143be21a954231370e3a02ce03088994
          assets_archive="$RUNNER_TEMP/ppsspp-assets-${assets_commit}.tar.gz"
          assets_extract="$RUNNER_TEMP/ppsspp-assets-${assets_commit}"

          curl --fail --location --retry 3 --retry-all-errors \
            "https://codeload.github.com/BadCaseDotOrg/PPSSPP_Assets/tar.gz/${assets_commit}" \
            --output "$assets_archive"
          mkdir -p "$assets_extract"
          tar -xzf "$assets_archive" -C "$assets_extract"

          flash0_source="$(find "$assets_extract" -type d -path '*/flash0' -print -quit)"
          test -n "$flash0_source"
          test -s "$flash0_source/font/ltn0.pgf"
          install -d assets
          cp -a "$flash0_source" assets/flash0
          test "$(sha256sum assets/flash0/font/ltn0.pgf | awk '{print $1}')" = \
            "1184f16bc89bad1fea49093806784470281af54f279b57ef0378ce33e6603870"

      - name: Restore vendored libpng ARM sources
        shell: bash
        run: |
          set -euo pipefail
          libpng_revision=09b7a626660a8a8a4c6b100518a21a17d0170e83
          libpng_arm_dir=ext/libpng17/arm
          mkdir -p "$libpng_arm_dir"

          for source_file in arm_init.c filter_neon.S filter_neon_intrinsics.c neon.h; do
            expected_blob="$(git -c safe.directory="$GITHUB_WORKSPACE" rev-parse \
              "${libpng_revision}:${libpng_arm_dir}/${source_file}")"
            git -c safe.directory="$GITHUB_WORKSPACE" show \
              "${libpng_revision}:${libpng_arm_dir}/${source_file}" > \
              "${libpng_arm_dir}/${source_file}"
            test "$(git -c safe.directory="$GITHUB_WORKSPACE" hash-object \
              "${libpng_arm_dir}/${source_file}")" = "$expected_blob"
          done

      - name: Build release NRO
        shell: bash
        run: |
          chmod +x build_local.sh build_ppsspp_GBAStation_nro.sh
          SWITCH_NVK_ROOT="$GITHUB_WORKSPACE/../switchVK/nvk-switch-26.1.4" \
            bash build_local.sh --clean -j "$(nproc)"
          test -s GBAStationPPSSPPStub.nro
          sha256sum GBAStationPPSSPPStub.nro > GBAStationPPSSPPStub.nro.sha256

      - name: Upload Nintendo Switch build
        uses: actions/upload-artifact@v4
        with:
          name: GBAStation-PPSSPP-Switch-${{ github.sha }}
          path: |
            GBAStationPPSSPPStub.nro
            GBAStationPPSSPPStub.nro.sha256
          if-no-files-found: error
          retention-days: 30

      - name: Publish GitHub Release assets
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            GBAStationPPSSPPStub.nro
            GBAStationPPSSPPStub.nro.sha256
